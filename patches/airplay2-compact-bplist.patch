diff --git a/src/airplay_crypto.cpp b/src/airplay_crypto.cpp
index 4de3ac9..d82b713 100644
--- a/src/airplay_crypto.cpp
+++ b/src/airplay_crypto.cpp
@@ -459,12 +459,14 @@ const Value* Value::find(const std::string& key) const {
 namespace {
 
 // bplist object-table encoder. We collect every distinct object into a flat
-// table, then emit it with a fixed 4-byte object-reference size (simpler and
-// always valid for the small dictionaries AirPlay SETUP uses). The format is
-// the canonical Apple bplist00: header "bplist00", object table, offset
-// table, then a 32-byte trailer.
+// table, then emit it with the smallest valid object-reference and offset
+// widths. Some AirPlay 2 receivers have narrow bplist parsers and expect the
+// canonical compact form generated by Apple's plist tools/libplist.
 struct Encoder {
-    std::vector<Bytes> objects;   // each object's encoded bytes (refs are 4-byte)
+    explicit Encoder(uint8_t objectRefSize) : objectRefSize(objectRefSize) {}
+
+    std::vector<Bytes> objects;
+    uint8_t objectRefSize;
 
     // Recursively flatten `v` into the object table, returning its index.
     int add(const Value& v) {
@@ -517,8 +519,8 @@ struct Encoder {
         return idx;
     }
 
-    static void appendRef(Bytes& obj, int ref) {
-        for (int s = 3; s >= 0; --s) obj.push_back(uint8_t((ref >> (8*s)) & 0xFF));
+    void appendRef(Bytes& obj, int ref) const {
+        for (int s = objectRefSize - 1; s >= 0; --s) obj.push_back(uint8_t((ref >> (8*s)) & 0xFF));
     }
 
     static void encodeMarkerLen(Bytes& obj, uint8_t marker, size_t len) {
@@ -554,11 +556,23 @@ struct Encoder {
     }
 };
 
+uint8_t widthForValue(uint64_t value) {
+    if (value <= 0xFF) return 1;
+    if (value <= 0xFFFF) return 2;
+    if (value <= 0xFFFFFFFFULL) return 4;
+    return 8;
+}
+
 } // namespace
 
 Bytes encode(const Value& root) {
-    Encoder enc;
+    Encoder enc(1);
     enc.add(root);   // root is object 0
+    const auto refSize = widthForValue(enc.objects.empty() ? 0 : enc.objects.size() - 1);
+    if (refSize != enc.objectRefSize) {
+        enc = Encoder(refSize);
+        enc.add(root);
+    }
 
     Bytes out;
     const char hdr[] = "bplist00";
@@ -572,17 +586,16 @@ Bytes encode(const Value& root) {
         out.insert(out.end(), o.begin(), o.end());
     }
 
-    // Offset table (use 4-byte offsets, generous for our small payloads).
     const uint64_t offsetTableStart = out.size();
-    const uint8_t offsetSize = 4;
+    const uint8_t offsetSize = widthForValue(offsets.empty() ? 0 : offsets.back());
     for (uint64_t off : offsets)
-        for (int s = 3; s >= 0; --s) out.push_back(uint8_t((off >> (8*s)) & 0xFF));
+        for (int s = offsetSize - 1; s >= 0; --s) out.push_back(uint8_t((off >> (8*s)) & 0xFF));
 
     // Trailer: 6 unused, offsetIntSize, objectRefSize, numObjects(8),
     // topObject(8), offsetTableOffset(8).
     Bytes trailer(32, 0);
     trailer[6] = offsetSize;
-    trailer[7] = 4;   // object refs are 4-byte (matches appendRef)
+    trailer[7] = refSize;
     auto put64 = [&](int at, uint64_t v) {
         for (int s = 0; s < 8; ++s) trailer[at + s] = uint8_t((v >> (8*(7-s))) & 0xFF);
     };
